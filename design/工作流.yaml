---
# 接口自动化工作流描述文件
- moduleName: 测试模块1
  owner: test01
  business: youzan-meiye
  workflow:
    - flowName: 111
      # 看看怎么优雅的生成测试数据，而不用手动录入
      data: id
      type: workflow
      flowApiList:
      - apiName: apiName
        apiId: "aaa"
        config:
          retry: 5
          timeout: 5000
        dependency:
        - name: m1.flow1.api1
          extractName: token_type
          dataSourceType: realRun / external / workflow
          inject:
            part: header / body / url
            location: Authorization / registInfo.reportno / taskId
        extract:
        - name: 111
          value: 111
    - flowName: 222
      data: id
      type: apiFullWithSituation
      flowApiList:
      - apiName: apiName
        apiId: "https"
        rules: []

